# Public env vars for the Vite client build (Cloudflare Pages compatible).
# Configure these in Cloudflare Pages -> Settings -> Environment variables.

# WhatsApp group invite URL used by CTA buttons.
VITE_WHATSAPP_GROUP_LINK="https://chat.whatsapp.com/your-invite-code"
